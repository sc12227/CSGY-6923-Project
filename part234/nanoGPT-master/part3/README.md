`part3_ans.csv` contains all the RNN parameters and performance metrics.

All log files contain training logs of various sizes.

`plot_rnn_vs_transformer_scaling.py` is used to generate performance comparisons of different-sized RNNs and a performance comparison with nanoGPT.
